import { ArmourFilterPipePipe } from './armour-filter-pipe.pipe';

describe('ArmourFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ArmourFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
